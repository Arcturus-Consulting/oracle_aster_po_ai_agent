# Copy this file to .env and fill the values.

APP_HOST=127.0.0.1
APP_PORT=8000

GEMINI_API_KEY=AQ.Ab8RN6LsMCN9EbO82KFsuU_g3YZg-iPnLD37TaS5R8pHo0GhBQ
GEMINI_MODEL=gemini-2.5-flash

ORACLE_BASE_URL=https://dabiqy.ds-fa.oraclepdemos.com/
ORACLE_REST_VERSION=11.13.18.05
ORACLE_AUTH_MODE=basic
ORACLE_USERNAME=CALVIN.ROTH
ORACLE_PASSWORD=mo*2a7*L

# OAuth only, if your Fusion instance uses OAuth instead of basic auth.
ORACLE_TOKEN_URL=
ORACLE_CLIENT_ID=
ORACLE_CLIENT_SECRET=

ORACLE_TIMEOUT_SECONDS=30
ORACLE_PAGE_LIMIT=50
ORACLE_MAX_PAGES=5

# Optional. Set this to match a test day, or leave empty to use machine date.
# Format: YYYY-MM-DD
TODAY_DATE=

# Optional email sending. If SMTP_HOST is empty, the Mail button returns a draft/mailto link only.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true
